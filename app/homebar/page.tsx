import TopSection from "../_components/TopSection";

export default async function HomebarMainPage() {
  return (
    <TopSection
      title={"(예정) Homebar"}
      content={"주류관련 작성된 글을 모아놓은 공간입니다."}
    />
  );
}
