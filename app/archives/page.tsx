import TopSection from "../_components/TopSection";

export default async function ArchivesMainPage() {
  return (
    <TopSection
      title={"(예정) Archives"}
      content={"🔎 모든 기록물들을 한 곳에서 볼 수 있는 곳 입니다."}
    />
  );
}